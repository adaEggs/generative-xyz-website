import React from 'react';
import s from './styles.module.scss';

const BTCGenerativeDetail: React.FC = (): React.ReactElement => {
  return (
    <div className={s.btcGenerativeDetail}>
      <p>btcGenerativeDetail</p>
    </div>
  );
};

export default BTCGenerativeDetail;
